apiVersion: v1
kind: Pod
metadata:
  name: gpu-toleration-pod
  labels:
    demo: toleration
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    ports:
    - containerPort: 80
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "gpu"
    effect: "NoSchedule"
